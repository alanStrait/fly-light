# Cargo build artifacts
/target/
**/*.rs.bk

# Remove Cargo.lock from gitignore if it's a library
# More info: https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
# Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of Rust's standard library may require these to be excluded
# depending on how they're compiled.
# 
# build/
